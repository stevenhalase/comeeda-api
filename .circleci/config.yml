version: 2
jobs:
  build:
    docker:
      image: circleci/node:latest
    steps:
      - checkout
      - run: echo "hello world"
deployment:
  staging:
    branch: master
    heroku:
      appname: comeeda-api
